import React, { useState, useEffect } from 'react'

function App() {
  return (
    <div>
//To test the app, make sure to run port 5000 first on server side then run the proxy server on port 3000
    </div>
  )
}

export default App